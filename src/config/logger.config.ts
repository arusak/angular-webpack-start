import {LoggerConfig} from "../app/core/models/logger-config";
export const tmLoggerConfig: LoggerConfig = {
  showRequests: true,
  showRedirects: true,
  showServices: [
    // 'Auth',
  ],
  showComponents: [
  ]
};
